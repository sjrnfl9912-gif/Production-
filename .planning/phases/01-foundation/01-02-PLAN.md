---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
autonomous: true
requirements:
  - CODE-02
  - CODE-03

must_haves:
  truths:
    - "AppState 전역 객체가 존재하며 AppState.tasks 배열로 행 상태가 관리된다"
    - "getRows()/setRows() 함수가 AppState.tasks를 읽고 쓴다 (dataset.rows 참조 없음)"
    - "window.confirm() 호출이 4곳 모두 await customConfirm()으로 교체되어 있다"
    - "customConfirm()이 Promise를 반환하고 기존 .modal-bg/.modal CSS를 재사용한다"
    - "window.alert() 호출이 없고 알림은 기존 showToast()를 사용한다"
    - "실적 입력 폼이 기존과 동일하게 동작한다 (추가/삭제/제출)"
    - "삭제 버튼을 누르면 커스텀 모달이 뜨고 확인/취소가 동작한다"
  artifacts:
    - path: "index.html"
      provides: "AppState object and state functions"
      contains: "const AppState"
    - path: "index.html"
      provides: "customConfirm Promise API"
      contains: "function customConfirm"
    - path: "index.html"
      provides: "modalContainer div in HTML"
      contains: "id=\"modalContainer\""
  key_links:
    - from: "deleteRecord() / removeWorker() / removeMainTask() / removeSubTask()"
      to: "customConfirm()"
      via: "await customConfirm(message) — 4개 함수가 async여야 함"
      pattern: "await customConfirm"
    - from: "setRows()"
      to: "AppState.tasks"
      via: "AppState.tasks = r; renderRows() 호출"
      pattern: "AppState\\.tasks"
    - from: "getRows()"
      to: "AppState.tasks"
      via: "return AppState.tasks"
      pattern: "return AppState\\.tasks"
---

<objective>
JS 상태를 DOM `data-rows` 속성에서 전역 `AppState` 객체로 마이그레이션하고, `window.confirm()`을 Promise 기반 커스텀 모달로 교체한다.

Purpose: `data-rows`는 DOM에 상태를 묶어 JS에서 직접 조작하기 어렵고, Phase 4 비즈니스 로직 개선 시 필터링/계산이 복잡해진다. `window.confirm()`은 iOS 카카오톡 웹뷰에서 차단되어 삭제 기능이 전혀 작동하지 않는 치명적 버그다.
Output: index.html — JS 상태관리 + 모달 교체 (CSS 변경 없음, 기능 동작 동일)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md

<interfaces>
<!-- 현재 JS 상태관리 코드 — 교체 대상 -->

현재 getRows/setRows 구현 (560~564줄):
```javascript
function getRows() { return JSON.parse(document.getElementById('taskTableBody').dataset.rows || '[]') }
function setRows(r) { document.getElementById('taskTableBody').dataset.rows = JSON.stringify(r); renderRows() }
function addRow() { const r=getRows(); r.push({id:++rowId,type:'메인작업',taskName:'',quantity:'',note:'',relatedMain:''}); setRows(r) }
function removeRow(id) { const r=getRows(); if(r.length<=1)return; setRows(r.filter(x=>x.id!==id)) }
function updateRow(id,f,v) { const r=getRows(),x=r.find(x=>x.id===id); if(x)x[f]=v; document.getElementById('taskTableBody').dataset.rows=JSON.stringify(r); if(f==='taskName'&&x&&x.type==='밑작업')renderRows() }
function changeType(id,t) { const r=getRows(),x=r.find(x=>x.id===id); if(x){x.type=t;x.taskName='';x.relatedMain=t==='밑작업'?'공통':''} setRows(r) }
```

현재 renderRows 구현 (545~558줄):
```javascript
function renderRows(){
  const tbody=document.getElementById('taskTableBody'), rows=JSON.parse(tbody.dataset.rows||'[]');
  tbody.innerHTML = rows.map(r => { ... }).join('');
}
```

window.confirm() 호출 위치 4곳:
- 674줄: `if(!confirm('정말 삭제하시겠습니까?'))return;` — deleteRecord()
- 1016줄: `if(!confirm(...))return;` — removeWorker()
- 1034줄: `if(!confirm(...))return;` — removeMainTask()
- 1095줄: `if(!confirm(...))return;` — removeSubTask()

기존 .modal-bg / .modal CSS (재사용):
```css
.modal-bg { position:fixed; top:0; left:0; right:0; bottom:0; background:rgba(0,0,0,.4); z-index:200; display:flex; align-items:center; justify-content:center; animation:fadeIn .2s }
.modal { background:#fff; border-radius:12px; box-shadow:0 20px 60px rgba(0,0,0,.2); width:90%; max-width:520px; max-height:90vh; overflow-y:auto; animation:slideUp .25s ease }
.modal-body { padding:24px }
.modal-footer { padding:16px 24px; border-top:1px solid var(--border-light); display:flex; justify-content:flex-end; gap:8px }
```

기존 modalContainer 활용:
- `closeModal()` (682줄): `document.getElementById('modalContainer').innerHTML=''`
- openEditModal()이 이미 `document.getElementById('modalContainer')` 사용
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AppState 마이그레이션 — data-rows → AppState.tasks</name>
  <files>index.html</files>
  <read_first>
    - index.html (JS 섹션 전체: 450~585줄 — 현재 getRows/setRows/renderRows/addRow/removeRow/updateRow/changeType 구현 확인)
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 3: AppState 마이그레이션)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-05, D-06, D-07)
  </read_first>
  <action>
index.html의 JS 상태관리 코드를 AppState 패턴으로 교체한다.

**Step 1 — AppState 객체 선언 추가 (JS 상단, rowId 선언 근처):**
```javascript
// ═══ SECTION: State Management ═══
const AppState = {
  tasks: [],  // { id, type, taskName, quantity, note, relatedMain }
};
```
`rowId` 변수 선언 직후 또는 JS 상단부에 추가한다.

**Step 2 — getRows() 교체:**
```javascript
// 기존 시그니처 유지 — 호출 측 코드 변경 없음
function getRows() {
  return AppState.tasks;
}
```

**Step 3 — setRows() 교체:**
```javascript
function setRows(r) {
  AppState.tasks = r;
  renderRows();
}
```
`document.getElementById('taskTableBody').dataset.rows` 참조 제거.

**Step 4 — renderRows() 수정:**
함수 첫 줄에서 `const rows = JSON.parse(tbody.dataset.rows||'[]')` → `const rows = AppState.tasks;` 로 교체.
`tbody.dataset.rows` 참조를 모두 제거한다.

**Step 5 — updateRow() 수정:**
함수 내 `document.getElementById('taskTableBody').dataset.rows=JSON.stringify(r)` 직접 쓰기를 제거하고 `AppState.tasks = r;` 로 교체한다.
```javascript
function updateRow(id, f, v) {
  const r = getRows(), x = r.find(x => x.id === id);
  if (x) x[f] = v;
  AppState.tasks = r;
  if (f === 'taskName' && x && x.type === '밑작업') renderRows();
}
```

**Step 6 — DOMContentLoaded init 확인:**
`addRow()` 호출 시 AppState.tasks가 초기화되어 있어야 한다. `addRow()` 로직은 변경 없음(이미 getRows/setRows 사용).

**Step 7 — taskTableBody의 data-rows 초기 속성 제거 (HTML):**
HTML에서 `<tbody id="taskTableBody" data-rows="[]">` 같은 초기값이 있으면 `data-rows` 속성 제거. JS에서 AppState.tasks로 초기화되므로 불필요.

**주의:**
- addRow(), removeRow(), changeType()은 이미 getRows()/setRows()를 통해 동작하므로 별도 수정 불필요.
- handleSubmit()의 `const rows = getRows().filter(...)` 도 getRows() 시그니처가 동일하므로 변경 없음.
- 코드 구조는 최대한 유지 (D-07: 클래스/모듈 불필요, 단순 객체+함수).
  </action>
  <verify>
    <automated>grep -n "AppState" index.html</automated>
    결과가 최소 5줄 이상 (선언 1 + getRows 1 + setRows 1 + renderRows 1 + updateRow 1)

    <automated>grep -n "dataset\.rows" index.html</automated>
    결과가 없어야 함 (data-rows 참조 완전 제거)
  </verify>
  <acceptance_criteria>
    - `grep "const AppState" index.html` 결과 존재
    - `grep "AppState.tasks" index.html | wc -l` 결과가 4 이상
    - `grep "dataset\.rows" index.html` 결과 없음
    - `grep "JSON.parse.*dataset\|JSON.stringify.*dataset" index.html` 결과 없음
    - 브라우저에서 실적 입력 탭 열어 행 추가/삭제/제출 동작 확인 (기능 동일)
  </acceptance_criteria>
  <done>
    - AppState.tasks 배열이 getRows()/setRows()의 실제 저장소로 사용된다
    - DOM `data-rows` 속성에 JSON을 저장하는 코드가 없다
    - 기존 addRow/removeRow/changeType/handleSubmit 기능이 동일하게 동작한다
  </done>
</task>

<task type="auto">
  <name>Task 2: customConfirm() — window.confirm() 4곳 교체</name>
  <files>index.html</files>
  <read_first>
    - index.html (JS 섹션: 600~700줄, 1010~1100줄 — confirm() 4개 호출 위치 확인)
    - index.html (HTML: modalContainer div 존재 여부 확인 — 없으면 추가 필요)
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 4: Promise-based confirm)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-08, D-09, D-10, D-11)
  </read_first>
  <action>
`window.confirm()` 호출 4곳을 `await customConfirm()` 으로 교체한다.

**Step 1 — HTML에 modalContainer 확인 및 추가:**
HTML body 내에 `<div id="modalContainer"></div>` 가 있는지 확인한다. 이미 openEditModal()이 사용 중이므로 존재할 가능성이 높다. 없으면 `</body>` 직전에 추가한다.

**Step 2 — customConfirm() 함수 추가 (JS Utilities 섹션):**
기존 `showToast()` 함수 근처(JS 유틸리티 섹션)에 추가한다:
```javascript
// ═══ SECTION: UI Utilities ═══
function customConfirm(message) {
  return new Promise((resolve) => {
    const container = document.getElementById('modalContainer');
    container.innerHTML = `
      <div class="modal-bg" id="confirmModal">
        <div class="modal" style="max-width:320px">
          <div class="modal-body" style="text-align:center;padding:28px 24px">
            <p style="font-size:15px;font-weight:600;margin-bottom:20px">${message}</p>
            <div style="display:flex;gap:8px;justify-content:center">
              <button class="btn btn-outline" id="confirmNo">취소</button>
              <button class="btn btn-danger" id="confirmYes">확인</button>
            </div>
          </div>
        </div>
      </div>`;
    document.getElementById('confirmYes').onclick = () => { container.innerHTML = ''; resolve(true); };
    document.getElementById('confirmNo').onclick  = () => { container.innerHTML = ''; resolve(false); };
  });
}
```
참고: customConfirm 내부의 style= 속성은 모달 크기 조정용 예외적 인라인 스타일로 허용한다 (max-width:320px, text-align:center 등 — 한 번만 쓰이는 특수 레이아웃).

**Step 3 — 4개 호출 함수를 async로 전환 + confirm() 교체:**

deleteRecord() (약 673줄):
```javascript
// 변경 전:
async function deleteRecord(id, isAdmin) {
  if (!confirm('정말 삭제하시겠습니까?')) return;

// 변경 후:
async function deleteRecord(id, isAdmin) {
  if (!await customConfirm('정말 삭제하시겠습니까?')) return;
```
(이미 async이므로 async 추가 불필요, confirm만 교체)

removeWorker() (약 1015줄):
```javascript
// 변경 전:
async function removeWorker(name) {
  if (!confirm(`"${name}" 작업자를 삭제하시겠습니까?`)) return;

// 변경 후:
async function removeWorker(name) {
  if (!await customConfirm(`"${name}" 작업자를 삭제하시겠습니까?`)) return;
```

removeMainTask() (약 1033줄):
```javascript
// 변경 전:
async function removeMainTask(name) {
  if (!confirm(`"${name}" 품목을 삭제하시겠습니까?`)) return;

// 변경 후:
async function removeMainTask(name) {
  if (!await customConfirm(`"${name}" 품목을 삭제하시겠습니까?`)) return;
```

removeSubTask() (약 1094줄):
```javascript
// 변경 전:
async function removeSubTask(name) {
  if (!confirm(`"${name}" 품목을 삭제하시겠습니까?`)) return;

// 변경 후:
async function removeSubTask(name) {
  if (!await customConfirm(`"${name}" 품목을 삭제하시겠습니까?`)) return;
```

**Step 4 — 검증:**
`window.confirm()` / `confirm(` 호출이 코드에 남아 있지 않은지 확인한다.

**주의:**
- `closeModal()` 함수가 이미 `document.getElementById('modalContainer').innerHTML=''` 를 하므로, customConfirm 모달 열린 상태에서 openEditModal()이 호출되면 충돌 가능. 단, 삭제와 수정이 동시에 호출되는 시나리오는 현재 UX 상 없으므로 무시해도 된다.
- `window.alert()` 는 현재 코드에 없고 `showToast()`로 이미 대체되어 있다 — 추가 작업 불필요 (D-10 이미 충족).
  </action>
  <verify>
    <automated>grep -n "confirm(" index.html | grep -v "customConfirm\|//\|customConfirm"</automated>
    결과가 없어야 함 (window.confirm 완전 제거)

    <automated>grep -n "customConfirm" index.html</automated>
    결과가 5줄 이상 (함수 선언 1 + 4개 호출)

    <automated>grep -n "function customConfirm" index.html</automated>
    결과가 정확히 1줄
  </verify>
  <acceptance_criteria>
    - `grep "if (!confirm\|if(!confirm" index.html` 결과 없음
    - `grep "await customConfirm" index.html | wc -l` 결과가 4
    - `grep "function customConfirm" index.html` 결과 1줄 존재
    - `grep 'id="modalContainer"' index.html` 결과 존재 (HTML에 div 있음)
    - 브라우저에서 삭제 버튼 클릭 시 커스텀 모달(확인/취소 버튼)이 표시됨
    - 모달에서 "취소" 클릭 시 삭제가 취소됨
    - 모달에서 "확인" 클릭 시 삭제가 실행됨
  </acceptance_criteria>
  <done>
    - `window.confirm()` 호출이 코드 어디에도 없다
    - `customConfirm()` Promise 함수가 정의되어 있다
    - 삭제 관련 4개 함수가 `await customConfirm()`을 사용한다
    - 커스텀 모달이 기존 `.modal-bg`/`.modal` CSS를 재사용하여 표시된다
  </done>
</task>

</tasks>

<verification>
Plan 02 완료 후 브라우저에서 확인:
1. 실적 입력 탭에서 행 추가(+) 클릭 → 행이 추가됨
2. 행 삭제 버튼 클릭 → 커스텀 모달 팝업 표시됨 (window.confirm 아님)
3. 모달 "취소" → 행 유지, "확인" → 행 삭제
4. 관리자 탭에서 작업자/품목 삭제 버튼 → 동일 커스텀 모달 표시
5. 실적 제출 후 AppState.tasks가 초기화되어 폼이 비워짐

자동화 확인:
- `grep "dataset\.rows" index.html` 결과 없음
- `grep "await customConfirm" index.html | wc -l` = 4
- `grep "if(!confirm\|if (!confirm" index.html` 결과 없음
</verification>

<success_criteria>
1. `AppState.tasks` 배열이 실적 입력 폼의 상태 저장소로 사용된다
2. `taskTableBody.dataset.rows` JSON 직렬화 코드가 없다
3. `window.confirm()` 호출이 없다
4. `customConfirm()` 함수가 Promise를 반환한다
5. 삭제 관련 4개 함수(deleteRecord, removeWorker, removeMainTask, removeSubTask)가 `await customConfirm()` 을 사용한다
6. 기존 실적 입력/수정/삭제/제출 기능이 모두 동작한다
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`

Summary must include:
- AppState 구조 (실제 구현된 형태)
- customConfirm() 함수 시그니처
- 교체된 4개 함수 목록
- 기능 회귀 테스트 결과
</output>
