---
phase: 05-pwa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - manifest.json
  - sw.js
  - icons/icon-192.png
  - icons/icon-512.png
  - index.html
autonomous: true
requirements:
  - PWA-01
  - PWA-02
  - PWA-03
  - PWA-04

must_haves:
  truths:
    - "Chrome/Safari에서 앱을 열면 홈 화면에 추가 가능하고 standalone 모드로 실행된다"
    - "재방문 시 index.html과 폰트가 Service Worker 캐시에서 즉시 로딩된다"
    - "카카오톡 웹뷰에서 앱을 열 때 Service Worker가 등록되지 않는다"
    - "네트워크가 끊기면 오프라인 배너가 표시되고, 복구 시 배너가 사라지며 데이터가 재로딩된다"
  artifacts:
    - path: "manifest.json"
      provides: "PWA 메타데이터 (name, icons, display, theme_color)"
      contains: "\"display\": \"standalone\""
    - path: "sw.js"
      provides: "Workbox 7.0.0 CDN 기반 Service Worker 캐싱"
      contains: "importScripts"
    - path: "icons/icon-192.png"
      provides: "192x192 PWA 아이콘"
    - path: "icons/icon-512.png"
      provides: "512x512 PWA 아이콘"
    - path: "index.html"
      provides: "manifest 링크, SW 등록 코드, KakaoTalk UA 가드, 오프라인 배너"
      contains: "rel=\"manifest\""
  key_links:
    - from: "index.html"
      to: "manifest.json"
      via: "<link rel=\"manifest\">"
      pattern: "rel=\"manifest\""
    - from: "index.html"
      to: "sw.js"
      via: "navigator.serviceWorker.register"
      pattern: "serviceWorker\\.register"
    - from: "index.html"
      to: "loadAll()"
      via: "window online event handler"
      pattern: "addEventListener.*online"
    - from: "sw.js"
      to: "Workbox CDN"
      via: "importScripts"
      pattern: "importScripts.*workbox"
---

<objective>
Phase 5 PWA 레이어 전체 구현: manifest.json + 아이콘 + Service Worker + KakaoTalk UA 가드 + 오프라인 UI

Purpose: 카카오톡 외 브라우저(Chrome, Safari)에서 홈 화면 추가/재방문 캐싱/오프라인 기본 UI를 제공한다. 카카오톡 웹뷰에서는 SW를 안전하게 스킵한다.
Output: manifest.json, sw.js, icons/icon-192.png, icons/icon-512.png 신규 생성 + index.html PWA 통합 코드 추가
</objective>

<execution_context>
@C:\Users\ADMIN\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\ADMIN\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-pwa/05-CONTEXT.md
@.planning/phases/05-pwa/05-RESEARCH.md

<interfaces>
<!-- Existing index.html structure the executor needs to know -->

Line 5: <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover, interactive-widget=resizes-content">
Line 6: <meta name="theme-color" content="#1e3a5f">
Line 7: <meta name="apple-mobile-web-app-capable" content="yes">
Line 8: <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
Line 9: <title>생산 관리 시스템</title>
Line 10-12: preconnect + Pretendard + Supabase CDN links

Line 16 CSS variables:
:root{--bg:#f0f1f3;--surface:#fff;--pri:#2563eb;--pri-dk:#1d4ed8;--danger:#dc2626;...}

Header gradient: background:linear-gradient(135deg,#1e3a5f,#2563eb)

Line 276: <body>
Line 277: <header class="header">

Line 473: async function loadAll(){ ... }
Line 1239: document.addEventListener('DOMContentLoaded', async()=>{ ... });
Line 1302: } // end initKeyboardHandler
Line 1303: </script>
Line 1304: </body>
Line 1305: </html>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: manifest.json + PWA 아이콘 생성</name>
  <files>manifest.json, icons/icon-192.png, icons/icon-512.png</files>
  <read_first>
    - index.html (lines 1-16: head 메타 태그, CSS 변수 확인)
    - .planning/phases/05-pwa/05-RESEARCH.md (Pattern 4: manifest.json 구조)
  </read_first>
  <action>
1. `icons/` 디렉토리를 프로젝트 루트에 생성

2. 플레이스홀더 PNG 아이콘 2개 생성 (per D-05). Canvas API로 텍스트 기반 아이콘을 만드는 Node.js 스크립트를 임시 실행하거나, 최소 유효 PNG 파일을 생성한다.
   - `icons/icon-192.png`: 192x192px, 배경색 `#1e3a5f` (헤더 그라데이션 시작색), 중앙에 흰색 "생산" 텍스트
   - `icons/icon-512.png`: 512x512px, 동일 스타일
   - 방법: `node -e` 스크립트로 Canvas를 사용하거나, 최소한 유효한 PNG 바이너리 파일 생성. node canvas 패키지가 없으면 1x1 solid color PNG를 base64 디코딩하여 생성해도 됨 (placeholder 목적이므로 크기보다 유효성이 중요)

3. `manifest.json` 파일을 프로젝트 루트에 생성:
```json
{
  "name": "생산 관리 시스템",
  "short_name": "생산관리",
  "description": "공장 일일 생산 실적 입력 및 대시보드",
  "start_url": "/",
  "display": "standalone",
  "orientation": "portrait",
  "background_color": "#ffffff",
  "theme_color": "#1e3a5f",
  "lang": "ko",
  "icons": [
    {
      "src": "/icons/icon-192.png",
      "type": "image/png",
      "sizes": "192x192",
      "purpose": "any maskable"
    },
    {
      "src": "/icons/icon-512.png",
      "type": "image/png",
      "sizes": "512x512",
      "purpose": "any maskable"
    }
  ]
}
```

Note: `theme_color`는 기존 `<meta name="theme-color" content="#1e3a5f">`와 일치시킴 (per D-06 discretion, 헤더 그라데이션 시작색 사용). `background_color`는 `#ffffff` (앱 기본 배경).
  </action>
  <verify>
    <automated>node -e "const m=JSON.parse(require('fs').readFileSync('manifest.json','utf8')); console.assert(m.display==='standalone'); console.assert(m.theme_color==='#1e3a5f'); console.assert(m.icons.length===2); console.assert(m.icons[0].sizes==='192x192'); console.assert(m.icons[1].sizes==='512x512'); const fs=require('fs'); console.assert(fs.existsSync('icons/icon-192.png')); console.assert(fs.existsSync('icons/icon-512.png')); console.log('PASS: manifest.json + icons verified')"</automated>
  </verify>
  <acceptance_criteria>
    - manifest.json 파일이 프로젝트 루트에 존재
    - manifest.json에 `"display": "standalone"` 포함
    - manifest.json에 `"theme_color": "#1e3a5f"` 포함
    - manifest.json에 192x192, 512x512 아이콘 2개 정의
    - icons/icon-192.png 파일 존재 (유효한 PNG)
    - icons/icon-512.png 파일 존재 (유효한 PNG)
  </acceptance_criteria>
  <done>manifest.json이 W3C PWA manifest 필수 필드를 모두 포함하고, 아이콘 파일 2개가 존재</done>
</task>

<task type="auto">
  <name>Task 2: sw.js Service Worker 생성 (Workbox 7.0.0 CDN)</name>
  <files>sw.js</files>
  <read_first>
    - .planning/phases/05-pwa/05-RESEARCH.md (sw.js 전체 코드 예시, Anti-Patterns)
    - .planning/phases/05-pwa/05-CONTEXT.md (D-03: Supabase API network-only, D-04: index.html 전략 discretion)
    - CLAUDE.md (Workbox 7.0.0 CDN URL 확인)
  </read_first>
  <action>
프로젝트 루트에 `sw.js` 파일 생성. Workbox 7.0.0 CDN `importScripts` 방식.

중요 결정사항:
- D-03 (LOCKED): Supabase API는 캐싱하지 않음. `workbox.strategies.NetworkOnly` 사용 (NetworkFirst 아님)
- D-04 (discretion): index.html은 `NetworkFirst` 전략 사용 — 온라인 시 최신 버전, 오프라인 시 캐시 폴백. CacheFirst는 단일 파일 앱에서 업데이트 지연을 유발하므로 부적합.

```javascript
// sw.js — Workbox 7.0.0 CDN Service Worker
importScripts('https://storage.googleapis.com/workbox-cdn/releases/7.0.0/workbox-sw.js');

// 새 SW 즉시 활성화
workbox.core.skipWaiting();
workbox.core.clientsClaim();

// index.html (navigation request) — NetworkFirst 5초 타임아웃
// D-04 discretion: 온라인 시 최신 버전 제공, 오프라인 시 캐시 폴백
workbox.routing.registerRoute(
  ({request}) => request.mode === 'navigate',
  new workbox.strategies.NetworkFirst({
    cacheName: 'pages-v1',
    networkTimeoutSeconds: 5,
    plugins: [
      new workbox.expiration.ExpirationPlugin({ maxAgeSeconds: 7 * 24 * 60 * 60 }),
    ],
  })
);

// 폰트 (Pretendard CDN) — CacheFirst 30일
workbox.routing.registerRoute(
  ({request}) => request.destination === 'font',
  new workbox.strategies.CacheFirst({
    cacheName: 'fonts-v1',
    plugins: [
      new workbox.expiration.ExpirationPlugin({ maxAgeSeconds: 30 * 24 * 60 * 60 }),
    ],
  })
);

// Supabase API — NetworkOnly (D-03: 캐싱 안 함)
workbox.routing.registerRoute(
  ({url}) => url.hostname.includes('supabase.co'),
  new workbox.strategies.NetworkOnly()
);
```

주의사항:
- sw.js는 반드시 프로젝트 루트에 배치 (scope 문제 방지)
- `skipWaiting()` + `clientsClaim()` 쌍으로 즉시 활성화
- Supabase URL 매칭: `url.hostname.includes('supabase.co')` — supabase 프로젝트 URL 패턴에 맞음
- CDN 스크립트(Supabase JS, Pretendard CSS)는 별도 캐싱 규칙 없이 브라우저 기본 HTTP 캐시에 위임
  </action>
  <verify>
    <automated>node -e "const sw=require('fs').readFileSync('sw.js','utf8'); console.assert(sw.includes('importScripts')); console.assert(sw.includes('workbox-cdn/releases/7.0.0')); console.assert(sw.includes('skipWaiting')); console.assert(sw.includes('clientsClaim')); console.assert(sw.includes('NetworkFirst')); console.assert(sw.includes('CacheFirst')); console.assert(sw.includes('NetworkOnly')); console.assert(sw.includes('supabase.co')); console.assert(!sw.includes('NetworkFirst') || !sw.match(/supabase.*NetworkFirst/s)); console.log('PASS: sw.js verified')"</automated>
  </verify>
  <acceptance_criteria>
    - sw.js 파일이 프로젝트 루트에 존재
    - sw.js에 `importScripts('https://storage.googleapis.com/workbox-cdn/releases/7.0.0/workbox-sw.js')` 포함
    - sw.js에 `workbox.core.skipWaiting()` 포함
    - sw.js에 `workbox.core.clientsClaim()` 포함
    - navigation request에 `NetworkFirst` 전략 사용
    - font request에 `CacheFirst` 전략 사용
    - supabase.co URL에 `NetworkOnly` 전략 사용 (D-03)
  </acceptance_criteria>
  <done>sw.js가 Workbox 7.0.0 CDN으로 navigation=NetworkFirst, font=CacheFirst, Supabase=NetworkOnly 전략을 구현</done>
</task>

<task type="auto">
  <name>Task 3: index.html PWA 통합 (manifest 링크 + SW 등록 + KakaoTalk 가드 + 오프라인 배너)</name>
  <files>index.html</files>
  <read_first>
    - index.html (lines 1-16: head 메타 태그, lines 276-280: body 시작, lines 1239-1305: DOMContentLoaded + script 끝)
    - .planning/phases/05-pwa/05-CONTEXT.md (D-01, D-02, D-07 결정사항)
    - .planning/phases/05-pwa/05-RESEARCH.md (Pattern 2: KakaoTalk UA Guard, Pattern 3: Offline UI)
  </read_first>
  <action>
index.html에 4가지 PWA 요소를 추가한다:

**A. `<head>` 태그 내 추가 (line 9 `<title>` 바로 아래)**

기존에 이미 있는 항목 확인:
- `<meta name="theme-color" content="#1e3a5f">` — 이미 존재 (line 6). 추가 불필요.
- `<meta name="apple-mobile-web-app-capable" content="yes">` — 이미 존재 (line 7). 추가 불필요.
- `<meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">` — 이미 존재 (line 8). 추가 불필요.

새로 추가할 항목 (line 9 `<title>` 태그 아래에 삽입):
```html
<link rel="manifest" href="/manifest.json">
<meta name="apple-mobile-web-app-title" content="생산관리">
<link rel="apple-touch-icon" href="/icons/icon-192.png">
```

**B. 오프라인 배너 HTML (line 277 `<header class="header">` 바로 위)**

```html
<div id="offline-banner" style="display:none;background:#dc2626;color:#fff;padding:8px 16px;text-align:center;font-size:14px;font-weight:600;position:sticky;top:0;z-index:1000">오프라인 상태입니다. 네트워크 연결을 확인해 주세요.</div>
```

배경색 `#dc2626` = CSS 변수 `--danger` 값. sticky top:0 + z-index:1000으로 헤더 위에 표시.

**C. PWA 초기화 JS (line 1303 `</script>` 바로 위에 삽입, `initKeyboardHandler()` 함수 정의 후)**

```javascript
// ═══ PWA: Service Worker 등록 + 오프라인 처리 ═══
(function initPWA(){
  // KakaoTalk UA 가드 (PWA-03, D-07)
  var isKakaoTalk=/KAKAOTALK/i.test(navigator.userAgent);
  if('serviceWorker' in navigator&&!isKakaoTalk){
    window.addEventListener('load',function(){
      navigator.serviceWorker.register('/sw.js')
        .then(function(reg){console.log('[SW] Registered:',reg.scope)})
        .catch(function(err){console.warn('[SW] Registration failed:',err)});
    });
  }
  // 오프라인 배너 (PWA-04, D-01: 최소한의 오프라인 처리)
  var banner=document.getElementById('offline-banner');
  function handleOffline(){if(banner)banner.style.display='block'}
  function handleOnline(){
    if(banner)banner.style.display='none';
    // D-02: 네트워크 복구 시 loadAll()로 데이터 재로딩 (사용자 새로고침 없이)
    if(typeof loadAll==='function')loadAll().catch(console.error);
  }
  window.addEventListener('offline',handleOffline);
  window.addEventListener('online',handleOnline);
  if(!navigator.onLine)handleOffline();
})();
```

주의사항:
- `var` 사용 (IIFE 내부이므로 const/let도 안전하지만 기존 코드 스타일과 맞춤)
- `/KAKAOTALK/i` 대소문자 무관 매칭
- `loadAll()` 직접 호출로 Supabase 데이터 재로딩 (D-02: 사용자가 새로고침 안 해도 자동 재로드)
- `typeof loadAll==='function'` 가드로 안전한 호출
  </action>
  <verify>
    <automated>node -e "const h=require('fs').readFileSync('index.html','utf8'); console.assert(h.includes('rel=\"manifest\"')); console.assert(h.includes('apple-mobile-web-app-title')); console.assert(h.includes('apple-touch-icon')); console.assert(h.includes('offline-banner')); console.assert(h.includes('KAKAOTALK')); console.assert(h.includes('serviceWorker.register')); console.assert(h.includes('addEventListener') && h.includes('offline')); console.assert(h.includes('addEventListener') && h.includes('online')); console.assert(h.includes('loadAll')); console.log('PASS: index.html PWA integration verified')"</automated>
  </verify>
  <acceptance_criteria>
    - index.html `<head>`에 `<link rel="manifest" href="/manifest.json">` 포함
    - index.html `<head>`에 `<meta name="apple-mobile-web-app-title" content="생산관리">` 포함
    - index.html `<head>`에 `<link rel="apple-touch-icon" href="/icons/icon-192.png">` 포함
    - index.html `<body>` 시작부에 `id="offline-banner"` div 존재
    - offline-banner에 `display:none` 기본 스타일 (초기 숨김)
    - JS에 `/KAKAOTALK/i.test(navigator.userAgent)` UA 감지 코드 포함
    - JS에 `navigator.serviceWorker.register('/sw.js')` 포함
    - JS에 `!isKakaoTalk` 조건으로 SW 등록이 가드됨
    - JS에 `window.addEventListener('offline', ...)` 포함
    - JS에 `window.addEventListener('online', ...)` 포함
    - online 핸들러에서 `loadAll()` 호출
    - 기존 `<meta name="theme-color">`, `<meta name="apple-mobile-web-app-capable">` 중복 추가 없음
  </acceptance_criteria>
  <done>index.html이 manifest 링크, SW 등록(KakaoTalk 가드), 오프라인 배너/핸들러를 모두 포함하고, 기존 메타 태그와 충돌 없음</done>
</task>

</tasks>

<verification>
1. `node -e` 검증 스크립트로 manifest.json, sw.js, index.html 내용 확인
2. `icons/icon-192.png`, `icons/icon-512.png` 파일 존재 확인
3. index.html에서 기존 meta 태그 중복 없음 확인 (theme-color, apple-mobile-web-app-capable 1회씩만)
</verification>

<success_criteria>
- manifest.json이 유효하고 standalone display + 192/512 아이콘 정의
- sw.js가 Workbox 7.0.0 CDN으로 NetworkFirst(pages) + CacheFirst(fonts) + NetworkOnly(Supabase) 구현
- index.html에 manifest 링크 + SW 등록 + KakaoTalk UA 가드 + 오프라인 배너/핸들러 포함
- 카카오톡 웹뷰에서 SW 등록이 스킵됨 (UA 가드)
- 오프라인 시 배너 표시, 온라인 시 배너 숨김 + loadAll() 호출
</success_criteria>

<output>
After completion, create `.planning/phases/05-pwa/05-01-SUMMARY.md`
</output>
