---
phase: 05-pwa
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified: []
autonomous: false
requirements:
  - PWA-01
  - PWA-02
  - PWA-03
  - PWA-04

must_haves:
  truths:
    - "Chrome에서 앱을 열면 Lighthouse PWA audit에서 installable 판정을 받는다"
    - "카카오톡 웹뷰에서 앱을 열 때 SW 미등록 확인 (콘솔에 [SW] Registered 없음)"
    - "오프라인 토글 시 배너가 표시되고 온라인 복구 시 사라진다"
  artifacts: []
  key_links: []
---

<objective>
Phase 5 PWA 전체 기능 인수 검증

Purpose: Plan 01에서 구현한 PWA 기능이 실제 브라우저에서 정상 동작하는지 사용자가 확인한다.
Output: 검증 완료 확인
</objective>

<execution_context>
@C:\Users\ADMIN\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\ADMIN\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-pwa/05-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: PWA 전체 기능 인수 검증</name>
  <files>manifest.json, sw.js, index.html</files>
  <action>
    사용자에게 PWA 기능 검증을 요청한다. 아래 체크리스트를 제시하고 결과를 확인한다.

    Phase 5 PWA 레이어 전체:
    - manifest.json + 192/512px 아이콘
    - sw.js (Workbox 7.0.0 CDN — NetworkFirst pages, CacheFirst fonts, NetworkOnly Supabase)
    - index.html: manifest 링크, SW 등록 + KakaoTalk UA 가드, 오프라인 배너/핸들러

    **PC Chrome 검증:**
    1. 앱을 Chrome에서 열기 (localhost 또는 배포 URL)
    2. DevTools > Application > Manifest — manifest.json이 파싱되고 아이콘이 표시되는지 확인
    3. DevTools > Application > Service Workers — sw.js가 "activated and running" 상태인지 확인
    4. DevTools > Application > Cache Storage — `pages-v1`, `fonts-v1` 캐시가 생성되었는지 확인
    5. DevTools > Network — Offline 체크박스 ON → "오프라인 상태입니다" 배너가 화면 상단에 표시되는지 확인
    6. Offline 체크박스 OFF → 배너가 사라지고 데이터가 자동으로 다시 로딩되는지 확인

    **카카오톡 웹뷰 검증 (실기기):**
    7. 카카오톡에서 앱 URL을 열기
    8. 정상 동작 확인 (기존 기능 깨짐 없음)
    9. DevTools remote debugging 가능하면: Application > Service Workers에 sw.js가 등록되지 않음 확인

    **선택적 검증:**
    - Lighthouse PWA audit 실행: DevTools > Lighthouse > PWA 카테고리 체크 > Generate report
  </action>
  <read_first>
    - .planning/phases/05-pwa/05-01-SUMMARY.md
  </read_first>
  <verify>사용자가 "approved" 입력 또는 발견된 문제 설명</verify>
  <acceptance_criteria>
    - Chrome DevTools Application 탭에서 manifest.json 파싱 성공
    - Chrome DevTools에서 sw.js가 activated 상태
    - 오프라인 토글 시 배너 표시/숨김 정상 동작
    - 카카오톡 웹뷰에서 기존 기능 깨짐 없음
  </acceptance_criteria>
  <done>사용자가 PC Chrome + 카카오톡 웹뷰에서 PWA 기능을 확인하고 "approved" 입력</done>
</task>

</tasks>

<verification>
사용자가 위 체크리스트를 확인하고 "approved" 입력
</verification>

<success_criteria>
- 사용자가 PC Chrome + 카카오톡 웹뷰에서 PWA 기능을 확인하고 승인
</success_criteria>

<output>
After completion, create `.planning/phases/05-pwa/05-02-SUMMARY.md`
</output>
