---
phase: 02-touch-ux
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified: []
autonomous: false
requirements: [TOUCH-01, TOUCH-02, TOUCH-03, TOUCH-04, TOUCH-05]
must_haves:
  truths:
    - btn-icon renders at 44x44px in mobile emulation
    - button tap shows scale press feedback
    - left/right swipe switches tabs
    - dashboard table snaps to column on horizontal scroll
    - vertical scroll is not blocked by swipe handler
  artifacts:
    - path: index.html
      provides: Phase 02 complete single file
      contains: SECTION Touch UX
  key_links:
    - from: Chrome DevTools
      to: index.html
      via: local server or direct file open
      pattern: N/A
---

<objective>
Phase 02 Touch UX 구현 전체를 Chrome DevTools 모바일 에뮬레이션에서 검증한다.

Purpose: 5개의 TOUCH 요구사항이 실제로 동작하는지 사람이 직접 확인한다. 스와이프 탭 전환과 수직 스크롤 공존 여부, 버튼 터치 피드백, 테이블 스냅이 예상대로 작동하는지 확인한다.
Output: 승인 또는 구체적인 실패 내역
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-touch-ux/02-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Phase 02 Touch UX 전체 검증</name>
  <files>N/A</files>
  <action>Human verification — no automated steps. See how-to-verify for manual check steps.</action>
  <what-built>
5개의 TOUCH UX 요구사항 구현:
- TOUCH-01: btn-icon(44px), week-nav-btn(44px), btn-sm(min-height:44px), settings-item .btn(min-height:44px) — @media(pointer:coarse)
- TOUCH-02: input, select, textarea에 touch-action:manipulation 추가
- TOUCH-03: 버튼 :active scale(0.94)/opacity(0.82) + btn-primary/btn-danger ripple ::after
- TOUCH-04: initSwipeNav() — 좌우 스와이프로 탭 전환 (MIN_X=60px, MAX_Y=80px, passive:true)
- TOUCH-05: .overflow-x에 scroll-snap-type:x proximity, .dash-table th에 scroll-snap-align:start
  </what-built>
  <how-to-verify>
준비: python -m http.server 8080 또는 Live Server로 index.html 서빙 후 Chrome에서 열기
Chrome DevTools: F12 → 디바이스 아이콘 → iPhone 14 Pro (390x844) 선택

TOUCH-01 — 터치 타겟 크기:
1. 실적 입력 탭 → 작업 행의 삭제 아이콘(btn-icon) 우클릭 → 검사 → Computed → width/height: 44px 이상
2. 주간 대시보드 → 주차 이동 버튼(week-nav-btn) 검사 → 44px x 44px 이상
3. 관리자 설정 → 작업자 "삭제" 버튼(settings-item .btn) 검사 → min-height: 44px 이상

TOUCH-02 — touch-action:
4. 실적 입력의 텍스트 입력 필드 검사 → Computed → touch-action: manipulation

TOUCH-03 — 시각적 피드백:
5. "실적 제출하기" 버튼(btn-primary) 클릭 → 눌리는 순간 scale 축소 + 흰 ripple 표시
6. "삭제" 버튼(btn-danger) 클릭 → ripple 표시
7. 일반 .btn 클릭 → scale(0.94) + opacity 어두워짐
8. 탭 버튼 탭 → 배경색 변화(pri-lt)

TOUCH-04 — 스와이프 탭 전환:
9. 화면 중앙에서 왼쪽으로 스와이프(60px 이상) → 다음 탭 전환
10. 오른쪽으로 스와이프 → 이전 탭 전환
11. 첫 탭(실적입력)에서 오른쪽 스와이프 → 탭 유지(전환 없음)
12. 마지막 탭(관리자)에서 왼쪽 스와이프 → 탭 유지(전환 없음)
13. 실적 입력 탭에서 위아래 스크롤 → 탭 전환 없이 수직 스크롤만
14. 관리자 PIN 입력 중 수평 스와이프 → 탭 전환 없음

TOUCH-05 — 테이블 스크롤 스냅:
15. 대시보드 CAPA 테이블 수평 스크롤 후 놓으면 컬럼 헤더 기준 스냅됨
16. 일생산현황(dailyReportArea) 테이블도 같은 스냅 동작

회귀 체크:
17. 4개 탭 모두 탭 버튼으로 전환 가능
18. F12 Console에 JS 에러 없음
  </how-to-verify>
  <verify>Human judgment — see how-to-verify steps above</verify>
  <done>All TOUCH-01 through TOUCH-05 pass manual verification. No regressions in tab switching or JS console.</done>
  <resume-signal>
모든 항목 통과 시: "approved"
실패 항목 있으면: 어떤 단계(번호)에서 무엇이 예상과 다른지 구체적으로 기술
  </resume-signal>
</task>

</tasks>

<verification>
Human checkpoint — see how-to-verify steps in Task 1
</verification>

<success_criteria>
- 5개 TOUCH 요구사항이 Chrome DevTools 모바일 에뮬레이션에서 동작한다
- 수직 스크롤이 스와이프 핸들러에 의해 차단되지 않는다
- Console JS 에러 없음
- 4탭 기능 100% 유지
</success_criteria>

<output>
완료 후 `.planning/phases/02-touch-ux/02-03-SUMMARY.md` 생성
</output>
