---
phase: 03-ui-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [index.html]
autonomous: true
requirements: [UI-02, UI-03, UI-04]

must_haves:
  truths:
    - "KPI 그리드가 360px 모바일에서 2열로 표시되고 400px+ 에서 3열로 전환된다"
    - "활성 탭 버튼 상단에 파란색 3px 인디케이터 바가 표시된다"
    - "토스트 메시지가 탭바 위에 표시되어 탭바에 가려지지 않는다"
    - "대시보드 테이블이 모바일에서 table-layout:auto로 컬럼을 자동 크기 조정한다"
    - "skeleton CSS 클래스 (.skeleton, .skeleton-line, .skeleton-card)와 @keyframes skeleton-shimmer가 index.html에 존재한다"
  artifacts:
    - path: "index.html"
      provides: "SECTION: Utilities에 skeleton 클래스 3종 + SECTION: Animations에 skeleton-shimmer keyframe + SECTION: Dashboard에 kpi-grid 2열 기본값 + SECTION: Layout에 tab-btn active indicator"
      contains: "skeleton-shimmer"
  key_links:
    - from: "SECTION: Dashboard .kpi-grid"
      to: "SECTION: Desktop Override @media(min-width:769px) .kpi-grid"
      via: "grid-template-columns override"
      pattern: "kpi-grid.*repeat\\(3"
    - from: ".tab-btn.active::before"
      to: ".tab-btn.active"
      via: "position:relative on .tab-btn"
      pattern: "tab-btn\\.active::before"
---

<objective>
CSS 전용 UI 개선 — KPI 그리드 2열화, 탭바 액티브 인디케이터, 토스트 위치 수정, 대시보드 테이블 모바일 최적화, 스켈레톤 CSS 기반 구축.

Purpose: Phase 3의 모든 시각적 변경 사항을 단일 CSS 블록 추가로 처리. JS 변경 없음 — 회귀 위험 최소화.
Output: index.html CSS 섹션 4곳 수정/추가 (Dashboard, Layout, Utilities, Animations).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ui-redesign/03-RESEARCH.md

<interfaces>
<!-- 수정 대상 CSS 규칙 — 현재 index.html에서 추출 -->

현재 SECTION 순서 (index.html):
  line 15: SECTION: CSS Variables
  line 18: SECTION: Reset & Base
  line 22: SECTION: Layout
  line 33: SECTION: Components
  line 68: SECTION: Forms
  line 79: SECTION: Tables
  line 99: SECTION: Dashboard
  line 126: SECTION: PIN
  line 160: SECTION: Utilities
  line 178: SECTION: Animations
  line 185: SECTION: Touch UX    ← Phase 2, 건드리지 말 것
  line 202: SECTION: Desktop Override

현재 .kpi-grid (line 100):
  .kpi-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:8px;margin-bottom:16px}

현재 데스크탑 오버라이드 .kpi-grid (line 232):
  .kpi-grid{grid-template-columns:repeat(3,1fr);gap:16px;margin-bottom:24px}

현재 .tab-btn (line 27):
  .tab-btn{flex:1;padding:8px 4px;font-size:10px;color:var(--text-muted);border-radius:8px;display:flex;flex-direction:column;align-items:center;gap:2px;line-height:1.2;border:none;cursor:pointer;transition:all .2s;background:0 0;white-space:nowrap;font-family:inherit;touch-action:manipulation}

현재 .tab-btn.active (line 29):
  .tab-btn.active{background:var(--pri-lt);color:var(--pri)}

현재 .toast (line 157):
  .toast{position:fixed;bottom:24px;left:50%;...}

현재 .dash-table (line 110):
  .dash-table{width:100%;border-collapse:collapse;font-size:12px;table-layout:fixed}

현재 데스크탑 .dash-table (line 238):
  .dash-table{font-size:13px;table-layout:auto}

현재 @keyframes (line 178-183):
  @keyframes fadeUp, shake, fadeIn, slideUp, spin

현재 SECTION: Utilities (line 160-176):
  .section-toolbar, .section-label, .card-inner, ... .full-height
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: KPI 2열 + 탭바 액티브 인디케이터 + 토스트 위치 CSS 수정</name>
  <files>index.html</files>
  <read_first>index.html (lines 22-32 SECTION:Layout, lines 99-125 SECTION:Dashboard, lines 157-158 .toast)</read_first>
  <action>
index.html의 세 곳을 수정한다.

**수정 1 — SECTION: Layout (.tab-btn 규칙)**

`.tab-btn` 규칙에 `position:relative` 추가 (기존 규칙 끝에 세미콜론으로 이어 붙임):
기존: `.tab-btn{flex:1;padding:8px 4px;...touch-action:manipulation}`
변경: `.tab-btn{flex:1;padding:8px 4px;...touch-action:manipulation;position:relative}`

`.tab-btn.active` 규칙 뒤에 새 규칙 추가:
```css
.tab-btn.active::before{content:'';position:absolute;top:2px;left:50%;transform:translateX(-50%);width:28px;height:3px;background:var(--pri);border-radius:0 0 4px 4px}
```

**수정 2 — SECTION: Dashboard (.kpi-grid)**

`.kpi-grid` 기본 규칙 변경:
기존: `.kpi-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:8px;margin-bottom:16px}`
변경: `.kpi-grid{display:grid;grid-template-columns:repeat(2,1fr);gap:8px;margin-bottom:16px}`

그 바로 아래에 400px 브레이크포인트 추가:
```css
@media(min-width:400px){.kpi-grid{grid-template-columns:repeat(3,1fr)}}
```

데스크탑 오버라이드 내 `.kpi-grid`는 이미 `repeat(3,1fr)` — 변경 불필요.

**수정 3 — SECTION: Components (.toast 규칙)**

`.toast` 규칙의 `bottom:24px`를 `bottom:calc(60px + env(safe-area-inset-bottom))`로 변경:
기존: `.toast{position:fixed;bottom:24px;left:50%;...}`
변경: `.toast{position:fixed;bottom:calc(60px + env(safe-area-inset-bottom));left:50%;...}`

데스크탑 오버라이드 (`@media(min-width:769px)`) 마지막에 추가:
```css
.toast{bottom:24px}
```
(데스크탑에서는 탭바가 없으므로 24px 복원)
  </action>
  <verify>
    <automated>grep -n "tab-btn.active::before" /c/Users/ADMIN/Production/index.html && grep -n "repeat(2,1fr)" /c/Users/ADMIN/Production/index.html && grep -n "min-width:400px" /c/Users/ADMIN/Production/index.html && grep -n "calc(60px + env(safe-area-inset-bottom))" /c/Users/ADMIN/Production/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep "tab-btn.active::before" index.html` → 결과 1줄 (mobile base 섹션 내)
    - `grep "repeat(2,1fr)" index.html` → 결과 1줄 (.kpi-grid 기본 규칙)
    - `grep "min-width:400px" index.html` → 결과 1줄 (kpi 3열 복원 브레이크포인트)
    - `grep "calc(60px + env(safe-area-inset-bottom))" index.html` → 결과 1줄 (.toast)
    - `grep "position:relative" index.html | grep "tab-btn"` → tab-btn 규칙에 position:relative 포함
    - 데스크탑 오버라이드 내 .kpi-grid 는 여전히 `repeat(3,1fr)` 유지
  </acceptance_criteria>
  <done>탭바 액티브 인디케이터, KPI 2열 모바일 기본값, 토스트 위치 수정이 index.html에 반영됨</done>
</task>

<task type="auto">
  <name>Task 2: 대시보드 테이블 모바일 최적화 + 스켈레톤 CSS 기반 구축</name>
  <files>index.html</files>
  <read_first>index.html (lines 99-125 SECTION:Dashboard, lines 160-177 SECTION:Utilities, lines 178-184 SECTION:Animations, lines 793-795 renderDailyReport inline widths)</read_first>
  <action>
index.html의 세 곳을 수정한다.

**수정 1 — SECTION: Dashboard (.dash-table 모바일 기본값)**

기존: `.dash-table{width:100%;border-collapse:collapse;font-size:12px;table-layout:fixed}`
변경: `.dash-table{width:100%;border-collapse:collapse;font-size:12px;table-layout:auto}`

(데스크탑 오버라이드 line 238에 이미 `table-layout:auto` 있음 — 변경 불필요)

`renderDailyReport()` 함수 내 inline style width 제거:
기존 (line ~794):
`let h=\`<table class="dash-table"><thead><tr><th style="width:100px">담당자</th><th style="width:70px">구분</th><th>작업명</th><th class="num" style="width:80px">수량</th><th>비고</th></tr></thead><tbody>\`;`
변경: 모든 `style="width:Xpx"` 속성 제거:
`let h=\`<table class="dash-table"><thead><tr><th>담당자</th><th>구분</th><th>작업명</th><th class="num">수량</th><th>비고</th></tr></thead><tbody>\`;`

**수정 2 — SECTION: Utilities 끝에 스켈레톤 CSS 추가**

`.full-height` 클래스 뒤(SECTION: Utilities 마지막)에 추가:
```css
.skeleton{background:var(--skeleton-base,#e9eaec);border-radius:6px;overflow:hidden;position:relative}
.skeleton::after{content:'';position:absolute;inset:0;background:linear-gradient(90deg,transparent 0%,rgba(255,255,255,0.5) 50%,transparent 100%);animation:skeleton-shimmer 1.4s ease infinite}
.skeleton-line{height:14px;margin-bottom:8px;border-radius:4px}
.skeleton-line.short{width:40%}.skeleton-line.medium{width:70%}.skeleton-line.full{width:100%}
.skeleton-card{background:var(--surface);border:1px solid var(--border-light);border-radius:var(--r);padding:14px;margin-bottom:10px}
```

**수정 3 — SECTION: Animations에 skeleton-shimmer keyframe 추가**

기존 `@keyframes spin` 뒤에 추가:
```css
@keyframes skeleton-shimmer{from{transform:translateX(-100%)}to{transform:translateX(100%)}}
```
  </action>
  <verify>
    <automated>grep -n "skeleton-shimmer" /c/Users/ADMIN/Production/index.html && grep -n "table-layout:auto" /c/Users/ADMIN/Production/index.html && grep -n "\.skeleton-card" /c/Users/ADMIN/Production/index.html && grep -c "style=\"width:" /c/Users/ADMIN/Production/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep "skeleton-shimmer" index.html` → 2줄 (CSS 클래스 참조 + keyframe 정의)
    - `grep "\.skeleton-card" index.html` → 1줄
    - `grep "table-layout:auto" index.html` → 2줄 (기본값 + 데스크탑 오버라이드)
    - `grep "table-layout:fixed" index.html` → 0줄 (완전 제거)
    - `grep "style=\"width:" index.html` → renderDailyReport의 th inline width가 0으로 줄었음 (이전 대비 감소)
    - SECTION: Animations 내에 skeleton-shimmer keyframe 위치 (Touch UX 섹션 이전)
  </acceptance_criteria>
  <done>스켈레톤 CSS 클래스 3종 + keyframe 추가, 대시보드 테이블 table-layout:auto로 모바일 컬럼 자동 크기 조정</done>
</task>

</tasks>

<verification>
두 태스크 완료 후 grep 확인:

```bash
# 탭 인디케이터
grep -n "tab-btn.active::before" index.html

# KPI 2열
grep -n "repeat(2,1fr)" index.html

# 400px 브레이크포인트
grep -n "min-width:400px" index.html

# 토스트 위치
grep -n "60px + env(safe-area-inset-bottom)" index.html

# 스켈레톤 CSS
grep -n "skeleton-shimmer\|skeleton-card\|skeleton-line" index.html

# table-layout 확인
grep -n "table-layout" index.html

# 회귀: 데스크탑 KPI 3열 유지
grep -n "kpi-grid" index.html

# SECTION 순서 유지 확인
grep -n "SECTION:" index.html
```
</verification>

<success_criteria>
- .kpi-grid 기본값이 repeat(2,1fr) — 360px 화면에서 2열 표시
- @media(min-width:400px) .kpi-grid 3열 복원 브레이크포인트 존재
- .tab-btn.active::before 존재 (파란 탭 인디케이터)
- .toast bottom이 탭바 위로 계산됨 (calc(60px + env(...)))
- .skeleton, .skeleton-line, .skeleton-card, @keyframes skeleton-shimmer 존재
- .dash-table table-layout:auto (기본값 및 데스크탑 오버라이드)
- index.html SECTION 순서 불변 (11 SECTION + Touch UX + Desktop Override)
- 데스크탑 오버라이드 .kpi-grid 여전히 repeat(3,1fr) (회귀 없음)
</success_criteria>

<output>
완료 후 `.planning/phases/03-ui-redesign/03-01-SUMMARY.md` 생성
</output>
